<UserControl
	x:Name="Control"
	x:Class="Laevo.View.Activity.ActivityControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:Converters="clr-namespace:Laevo.View.Activity.Converters"
    mc:Ignorable="d" 
    d:DesignHeight="118" d:DesignWidth="206">

	<UserControl.Resources>
		<Converters:ActivityColorConverter x:Key="ActivityColorConverter" />
	</UserControl.Resources>
	
	<Grid>
		
		<Grid.Background>
			<LinearGradientBrush StartPoint="0, 0" EndPoint="1, 0">
				<GradientStop Color="{Binding ElementName=Control, Path=Color}" Offset="1" />
				<GradientStop Color="{Binding ElementName=Control, Path=Color, Converter={StaticResource ActivityColorConverter}}" Offset="0" />				
			</LinearGradientBrush>
		</Grid.Background>

	</Grid>
	
</UserControl>
